const std = @import("std");

pub fn main() !void {
    std.debug.print("NVENC test tool - Integration with external ghostnv repo needed\n", .{});
    std.debug.print("This tool will interface with github.com/ghostkellz/ghostnv\n", .{});
}